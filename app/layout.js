import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
});

export const metadata = {
  title: "SIPERS – Social Interaction & Peer Engagement Rating Scale",
  description:
    "A professional digital assessment tool for clinicians and therapists to evaluate social interaction and peer engagement in children.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
